import { EnrollModelDialog } from './enroll-model-dialog';

describe('EnrollModelDialog', () => {
  it('should create an instance', () => {
    expect(new EnrollModelDialog()).toBeTruthy();
  });
});
